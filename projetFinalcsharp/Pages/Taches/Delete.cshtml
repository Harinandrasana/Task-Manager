@page
@model projetFinalcsharp.Pages.Taches.deleteModel
@{
    bool reussi = Model.Supprimer();
    if (reussi)
    {
        Response.Redirect("/Taches/Index"); 
    }
    else
    {
        Console.WriteLine("Echec de la suppression");
    }
}
    
